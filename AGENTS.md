This repo is to build a personal assistant agent for the user. 

I am learning to code and build with agents so this is a beginner project and we will iterate step by step.

We are building out a simple knowledge base in /knowledge which points to the other docs. 
Useful project information is in `docs/`; start with `docs/README.md`, which points to the other docs.

The main thing we are going to be doing is customising the robot to use the pi coding agent runtime as the offboard brain, hosted on Railway, instead of the current openclaw setup.

index.md?? 
